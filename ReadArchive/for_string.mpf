;------------------------------------DEF STRING 
DEF STRING [30] CHANCES="OOOOO"
DEF STRING [255] IMPRIME
DEF STRING [255] RESULT[1]
DEF STRING [255] RESULT2[100]
DEF STRING [255] PALAVRA
DEF STRING [255] ACERTOS
DEF STRING [255] TENTATIVA = ""
DEF STRING [255] TENTATIVA_TEMP = ""
;------------------------------------DEF INT
DEF INT ERROR
DEF INT II
DEF INT TT=0
DEF INT XX = 0
DEF INT TAM_PALAVRA
DEF INT CONT = 0
DEF INT CONT2 = 0
DEF INT CONT3 = 0
;------------------------------------AJUSTES
READ (ERROR,"PALA",1,1, RESULT)
PALAVRA = RESULT[0]
IMPRIME = PALAVRA
MSG(PALAVRA)
TAM_PALAVRA = (STRLEN (PALAVRA))
REPEAT INI_ACER FIM_ACER
REPEAT INI_IMP FIM_IMP
;-------------------------------------INI_PROG
R50=0
OO1:
IF R50 == 1 GOTOF OO2

DEV_ERRO_1:
MSG("DIGITE UMA LETRA")
M00
LER_LETRA:
       READ (ERROR,"LETRA",1,1, RESULT2)
IF RESULT2[0] == TENTATIVA GOTOB LER_LETRA 
;M00
;TENTATIVA_TEMP = RESULT2[0]
;MSG(RESULT2[0] << " " << TENTATIVA_TEMP  )  
;CONT3 = CONT3+1
;TT=TT+1

;M00
;IF RESULT2[CONT3] == TENTATIVA GOTOF INI_ERRO_1
TENTATIVA = RESULT2[0]
;MSG("" << TENTATIVA << "     " << RESULT2[0])
;M00
REPEAT INI_VERI FIM_VERI
;M00
REPEAT INI_IMP FIM_IMP
M00
;RESULT2[0] = "" 
;MSG(RESULT2[0])
R21=0
IF CONT2 == 5
       MSG("VOCE PERDEU ! ! !")
       R50=1
M00
ENDIF
IF CONT == TAM_PALAVRA
       MSG("VOCE VENCEU ! ! !")
       R50=1
M00
ENDIF

GOTOB OO1

INI_ERRO_1:
MSG("A LETRA JA FOI DIGITADA!")
M00
RESULT2[0] = "" 
GOTOB DEV_ERRO_1

OO2:
;-------------------------------------FIM_PROG
M30

INI_ACER:
FOR II=0 TO TAM_PALAVRA
      ACERTOS[II] = "_"
ENDFOR
FIM_ACER:


INI_IMP:
R20=0
FOR II=0 TO TAM_PALAVRA*2
       IF (II MOD 2)
              ;MSG("POSICAO " << II << "="<< PALAVRA[II])
              IMPRIME[II] =  ACERTOS[R20] 
              R20=R20+1
       ELSE
              IMPRIME[II] = " "
       ENDIF
ENDFOR
MSG("CHANCES: " << CHANCES << "  PALAVRA: " << IMPRIME)
FIM_IMP:

INI_VERI:
   FOR II=0 TO TAM_PALAVRA
;MSG("" << TENTATIVA << " " << (PALAVRA[II] ))
;M00
       IF (PALAVRA[II] == TENTATIVA)
MSG("" << TENTATIVA << " " << (PALAVRA[II] ))
;M00
              ACERTOS[II] = TENTATIVA
              R21=1
              CONT = CONT+1
       ENDIF
   ENDFOR
   IF R21 == 0
;       MSG("MMMMMMMMM")
       M00
       CHANCES[CONT2] = "X" 
       CONT2 = CONT2 +1
   ENDIF
FIM_VERI:

